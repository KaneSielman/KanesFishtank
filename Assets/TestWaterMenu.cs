using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;


public class TestWaterMenu : MonoBehaviour
{
    /*Check if "Test Water" button is clicked...
    public static bool WaterIsTesting = false;
    public GameObject testMenuUI;


    // Update is called once per frame
    void Update()
    {
        if (Input.GetKeyDown(KeyCode.Escape))
        {
            if (WaterIsTesting)
            {
                Resume();
            }
            else 
            {
                Pause();
            }
        }
    }
    void Resume()
    {
        testMenuUI.SetActive(false);
        Time.timeScale = 1f;
        WaterIsTesting = false;
    }
    void Pause()
    {
        testMenuUI.SetActive(true);
        Time.timeScale = 0f;
        WaterIsTesting = true;
    }*/
}